
## iMessage + BlueBubbles Setup Complete (Apr 19, 2026)
- BlueBubbles v1.9.9 installed at /Applications/BlueBubbles.app
- Firebase project: BlueBubblesApp (mira.novianintel@gmail.com)
- Config files saved: ~/Documents/Novian_Intelligence/bluebubbles/
- Server running on localhost:1234, password: M1r4154w350m3!
- **Auto-start: Login Item** (NOT launch agent — launch agent causes dock flash loop due to headless mode issues)
- SIP disabled on VM to enable Private API
- OpenClaw BlueBubbles channel: enabled in ~/.openclaw/openclaw.json
- **Webhook URL (critical):** `http://localhost:18789/bluebubbles-webhook?guid=M1r4154w350m3!`
  - The `?guid=` param is the BlueBubbles password — OpenClaw's custom extension validates this
  - Without it: 401 Unauthorized loop, messages received but not forwarded
  - Register via: `curl -X POST "http://localhost:1234/api/v1/webhook?password=M1r4154w350m3!" -H "Content-Type: application/json" -d '{"url": "http://localhost:18789/bluebubbles-webhook?guid=M1r4154w350m3!", "events": ["*"]}'`
- Andrei's number +15124706555 approved as trusted sender
- iMessage is now fully push-based and real-time via BlueBubbles ✅
- Menu bar icon: white SF Symbol "message" outline (Flutter scaling limitation, slight blur, acceptable)

## Debugging Notes (Apr 19 evening session)
- BlueBubbles launch agent causes dock flash loop — app starts then exits because headless=0 by default
- Setting headless=1 in config.db helps but Private API dylib injection into Messages.app crashes repeatedly on this VM
- Root cause of dylib crashes: macOS Sequoia 15.7.5 + VM environment rejects dylib injection even with SIP off
- **Working solution:** Login Item (not launch agent) + webhook with ?guid= auth param
- After any reboot, check webhook is still registered: `curl -s "http://localhost:1234/api/v1/webhook?password=M1r4154w350m3!" | python3 -m json.tool`
- If webhook is gone, re-register with the curl command above
